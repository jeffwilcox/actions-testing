name: Build the site
on:
  pull_request:
    types: [opened]
  issue_comment:
    types: [created, edited]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: xyz
        env:
          GHT: ${{secrets.GITHUB_TOKEN}}
        run: |
          echo "Hello world and $GHT"

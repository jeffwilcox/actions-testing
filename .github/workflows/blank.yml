name: Build the site
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: azure/docker-login@v1
        with:
          login-server: 'docker.pkg.github.com'
          username: 'jeffwilcox'
          password: ${{ secrets.GP_PASSWORD }}
      #image: docker.pkg.github.com/microsoft/opensource.microsoft.com/custom-jekyll-builder:latest
      #options: --user 1001
      #volumes:
      #  - /github/workflow:/usr/local/site
    #- uses: actions/checkout@v2
      - name: debug
        run: pwd && whoami
      # working-directory: /
    #- name: build
    #  run: |
    #    echo Gulping...
    #    echo ./node_modules/gulp/bin/gulp.js build
    #    echo Building the static site...
    #    echo JEKYLL_ENV=opensource.microsoft.com bundle exec jekyll build
